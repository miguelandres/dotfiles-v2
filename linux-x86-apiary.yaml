defaults:
  create:
    create_parent_dirs: true
  exec:
    echo: true
  link:
    force: true
    relink: true

steps:
  - subconfig:
      - linux.yaml
      - linux/intel-quicksync.yaml
      - docker-services/apiary-services.yaml
