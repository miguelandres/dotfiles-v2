defaults:
  link:
    force: true
    relink: true

steps:
  - link:
      - ~/.gitconfig: gitconfig-personal
      - ~/.gitignore_global: gitignore_global
      - ~/.commit-template.txt: commit-template.txt
  - exec:
      - jj config set --user user.name "$(git config user.name)"
      - jj config set --user user.email $(git config user.email)
      - jj config set --user git.push-bookmark-prefix "miguely-"
